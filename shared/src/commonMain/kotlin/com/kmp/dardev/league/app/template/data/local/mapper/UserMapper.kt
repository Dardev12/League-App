package com.kmp.dardev.league.app.template.data.local.mapper

import com.kmp.dardev.league.app.template.domain.model.User

/*fun UserEntity.toUser(): User {
    return User(
        UserId = UserGuid,
        Email = UserName,
        ProfilPicture = Picture
    )
}*/